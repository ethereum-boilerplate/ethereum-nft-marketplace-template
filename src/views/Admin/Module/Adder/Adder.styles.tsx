import styled from 'styled-components';

export const ModulesGridStyled = styled.div`
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(287px, 1fr));
    gap: 15px;
    margin-top: 16px;
    margin-bottom: 32px;
`;
