// import { Button } from 'antd'
// import { useState } from 'react'
// import { NFTCollection } from '../types/NFTCollection'
import { useMoralis } from 'react-moralis'
// import { ProjectAddress } from 'components/Admin'
export default function Roles() {
    // const { web3 } = useMoralis()
    // const getRoles = async () => {
    //     const DEFAULT_ADMIN_ROLE = await NFTCollection(web3, ProjectAddress).methods.DEFAULT_ADMIN_ROLE.call()
    //     const PAUSER_ROLE = await NFTCollection(web3, ProjectAddress).methods.PAUSER_ROLE.call()
    //     const TRANSFER_ROLE = await NFTCollection(web3, ProjectAddress).methods.TRANSFER_ROLE.call()
    //     const MINTER_ROLE = await NFTCollection(web3, ProjectAddress).methods.getRoleMemberCount(web3.utils.keccak256("MINTER_ROLE")).call()
    //     console.log(MINTER_ROLE)
    //     // const roleCount = await  NFTCollection(web3, ProjectAddress).methods.getRoleMemberCount().call()
    // }
    return (
        <div>
            COMING SOON
        </div>
    )
}
